# Auth state captured by global.setup.ts — real session cookies, never commit
auth/

# Node
node_modules/

# Playwright
test-results/
playwright-report/

# Archives
*.zip

# Claude Code local overrides (machine-specific)
.claude/settings.local.json
